"""API handler Lambda handler — placeholder for Phase 3 implementation."""


def lambda_handler(event, context):
    """Handle API Gateway proxy events."""
    return {
        "statusCode": 200,
        "headers": {"Content-Type": "application/json"},
        "body": "{}",
    }
